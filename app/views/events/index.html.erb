<p style="color: green"><%= notice %></p>

<h1>Eventos</h1>

<div id="events">
  <% @events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to "Ver esse evento", event %>
    </p>
  <% end %>
</div>

<%= link_to "Novo evento", new_event_path %>
